class Post {
  int id;
  int ownertType;
  int owner;
  String title;
  String description;
  String content;

  Post(this.id, this.ownertType, this.owner, this.title, this.content,
      this.description);
}
